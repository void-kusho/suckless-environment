# Compiled objects and binaries
*.o
utils/battery-notify/battery-notify
utils/brightness-notify/brightness-notify
utils/dmenu-session/dmenu-session
utils/dmenu-cpupower/dmenu-cpupower
utils/dmenu-clip/dmenu-clip
utils/dmenu-clipd/dmenu-clipd
utils/test-util
utils/test-dmenu
# Generated from config.def.h by install.sh / the Guix build (never commit)
utils/dmenu-session/config.h
utils/dmenu-cpupower/config.h

.claude
# Editor backups
*.swp
*.orig
*~
.planning/
# Guix build output symlink (generated by `guix build` / `guix system build`)
result

# channels-lock.scm is deliberately NOT ignored: it is the pin that keeps the
# VM and the laptop building the same dwl, and must be committed.
